meta {
  name: 02_login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: none
}

body:json {
  {
    "user": {
      "email": "demo@example.com",
      "password": "secret_password"
    }
  }
}

assert {
  res.status: oneOf [200, 401]
}
