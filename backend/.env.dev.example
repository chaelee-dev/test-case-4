# Conduit Backend — dev profile env template (ADR-0037 v1.1)
# Copy to backend/.env.dev and adjust values. NEVER commit backend/.env.*
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

DB_URL=jdbc:postgresql://localhost:5432/conduit_dev
DB_USER=conduit
DB_PASSWORD=conduit

JWT_SECRET=dev_only_change_me_64_bytes_min_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
JWT_EXPIRATION_HOURS=168

CORS_ALLOWED_ORIGINS=*
